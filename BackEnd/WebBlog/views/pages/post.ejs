<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog : <%= post.title %></title>

    <link rel="stylesheet" href="/styles/home.css">
    <link rel="stylesheet" href="/styles/editor.css">
    <link rel="stylesheet" href="/styles/post.css">

</head>
<body>
    <%- include('../partials/header')%>

    <div class="blog-banner">

        <img src="/uploads/<%= post.image %>" alt="<%= post.title %>" class="banner-img">

        <input type="file" accept="image/*" id="banner-upload" hidden>
    </div>
    
    <div class="blog">

       <h1 class="blog-title"> <%= post.title %> </h1>

       <p class="blog-article"> <%= post.article %> </p>

    </div>

    <div class="comments">
        <h2>Add a Comment</h2>
        <form action="/comment/<%= post.date %>" method="post">
            <input type="text" name="comment" placeholder="Type your comment here..." required>
            <button type="submit">Submit Comment</button>
        </form>
    </div>
    
    <% if (post.comments && post.comments.length > 0) { %>
        <h2>Comments:</h2>
        <ul>
            <% post.comments.forEach(comment => { %>
                <li><%= comment %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No comments yet. Be the first to comment!</p>
    <% } %>
    

    <script src="js/editor.js"></script>
  
</body>
</html>