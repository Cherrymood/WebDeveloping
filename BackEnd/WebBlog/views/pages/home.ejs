<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog : Homepage</title>

    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,400;0,500;0,600;1,600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="./styles/home.css">

</head>
<body>
    <%- include('../partials/header')%>
    
    <header class="header">
        <div class="content">
            <h1 class="heading">
                <span class="small">welcome in the world of</span>
                blog
                <span class="no-fill">writing</span>
            </h1>
            <% if (locals.isAuthenticated) {%>
                
                <a href="/editor" class="btn">write a blog</a>

            <%} else {%>

                <a href="/login" class="btn">write a blog</a>

            <% } %>
        </div>
    </header>

    <section class="articles">

        <h2 class="articles__heading">Latest Posts</h2>
      
        <ul class="article-ul">

          <% posts.forEach((post) => { %>
            <li>
              <a href="/post/<%= post.date %>">

                <span><%= post.title %></span>

                <span class="article-list__date"><%= new Date(post.date).toDateString() %></span>

              </a>

            </li>

          <% }) %>

        </ul>

        <%- include('../partials/footer')%>

<script src="js/home.js"></script>

</body>
</html>