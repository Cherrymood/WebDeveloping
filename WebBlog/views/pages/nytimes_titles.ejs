<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible"
        content="IE=edge">
    <meta name="viewport"
        content="width=device-width, initial-scale=1.0">
    <title>Blog : nytimes</title>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,400;0,500;0,600;1,600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet"
        href="styles/home.css">
    <link rel="stylesheet"
        href="styles/joke.css">
    <link rel="stylesheet"
        href="/styles/navbar.css">

</head>

<body>
    <%- include('../partials/header')%>

    <section class="articles">
        <h2 class="articles__heading">
            Articles</h2>

        <ul class="article-ul">
            <% if (!response) { %>
            <li>
                <p>No articles</p>
            </li>
            <% } else { %>
            <% for (let i = 0; i < response.length && i < 20; i++) { %>
            <li>
                <span>
                    <a href="<%= response[i].web_url %>"
                        target="_blank">

                        <%= response[i].headline.main %>
                    </a>
                </span>
                <span
                    class="article-list__date"><%= response[i].section_name %></span>
            </li>
            <% } %>
            <% } %>
        </ul>
    </section>

    <%- include('../partials/footer')%>

    <script src="js/home.js"></script>

</body>

</html>